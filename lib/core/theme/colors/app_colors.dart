import 'package:flutter/material.dart';

/// Base color palette for the application
class AppColorPalette {
  const AppColorPalette._();

  // Primary colors
  static const red = Color(0xFFEF4444);
  static const white = Color(0xFFFFFFFF);

  // Light theme colors
  static const lightBackground = Color(0xFFF9FAFB);
  static const lightPrimaryText = Color(0xFF111827);
  static const lightSecondaryText = Color(0xFF6B7280);
  static const lightTimerRing = Color(0xFFFCA5A5);
  static const lightBorder = Color(0xFFE5E7EB);

  // Dark theme colors
  static const darkBackground = Color(0xFF0F172A);
  static const darkPrimaryText = Color(0xFFF1F5F9);
  static const darkSecondaryText = Color(0xFF94A3B8);
  static const darkTimerRing = Color(0xFFDC2626);
  static const darkBorder = Color(0xFF1E293B);
}
